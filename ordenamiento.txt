ordenamiento alfabetico

int i,j,aux;

for(i=0;i<tam;i++)
{
	for(j=i+1;j<tam;j++)
	{
		if(strcmp(vec[i].nombre,vec[i].nombre)>0)
		{
			aux=vec[j];
			vec[j]=vec[i];
			vec[i]=aux;

		}

	}

}

ordenamiento


for(i=0;i<FIL-1;i++)
{
	for(j=i+1;j<FIL;j++)
	{
		if(vec[i]>vec[j])
		{
			aux=vec[i];
			vec[i]=vec[j];
			vec[j]=aux;
		}
	}
}




guardar en disco D

fecha estructura= son 5 estruc








